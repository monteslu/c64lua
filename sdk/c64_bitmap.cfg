# c64lua linker config: VIC-II multicolor bitmap mode in VIC bank 3.
#
# The .prg loads CONTIGUOUSLY from $0801 (a .prg cannot have gaps), so ALL code
# + data live in $0801-$BFFF like a stock cc65 c64 program - no reserved hole.
# The VIC-II reads its bitmap + screen from bank 3 ($C000-$FFFF), where the
# bitmap at $E000 sits UNDER the KERNAL ROM (the VIC sees the RAM beneath the
# ROM overlay - the classic C64 trick, costs nothing):
#
#   $C000-$C3FF  screen RAM (color-pair nibbles) + sprite pointers ($C3F8)
#   $E000-$FF3F  8000-byte multicolor bitmap (RAM under KERNAL)
#
# Those regions are ABOVE __HIMEM__ ($C000), so the linker never allocates
# there; the runtime addresses them directly (c64_api.c). Color RAM is fixed
# hardware at $D800. The KERNAL stays banked in for the frame loop (raster
# poll, I/O); the VIC reads bank-3 RAM regardless of the CPU's ROM banking.

FEATURES {
    STARTADDRESS: default = $0801;
}
SYMBOLS {
    __LOADADDR__:  type = import;
    __EXEHDR__:    type = import;
    __STACKSIZE__: type = weak, value = $0800;   # 2K cc65 C stack
    __HIMEM__:     type = weak, value = $C000;    # code/data stay below bank 3
}
MEMORY {
    ZP:       file = "", define = yes, start = $0002, size = $001A;
    LOADADDR: file = %O,               start = $07FF, size = $0002;
    HEADER:   file = %O, define = yes, start = $0801, size = $000D;
    MAIN:     file = %O, define = yes, start = __HEADER_LAST__, size = __HIMEM__ - __HEADER_LAST__;
    BSS:      file = "",               start = __ONCE_RUN__, size = __HIMEM__ - __STACKSIZE__ - __ONCE_RUN__;
}
SEGMENTS {
    ZEROPAGE: load = ZP,       type = zp;
    LOADADDR: load = LOADADDR, type = ro;
    EXEHDR:   load = HEADER,   type = ro;
    STARTUP:  load = MAIN,     type = ro;
    LOWCODE:  load = MAIN,     type = ro,  optional = yes;
    CODE:     load = MAIN,     type = ro;
    RODATA:   load = MAIN,     type = ro;
    DATA:     load = MAIN,     type = rw;
    INIT:     load = MAIN,     type = rw;
    ONCE:     load = MAIN,     type = ro,  define = yes;
    BSS:      load = BSS,      type = bss, define = yes;
}
FEATURES {
    CONDES: type    = constructor,
            label   = __CONSTRUCTOR_TABLE__,
            count   = __CONSTRUCTOR_COUNT__,
            segment = ONCE;
    CONDES: type    = destructor,
            label   = __DESTRUCTOR_TABLE__,
            count   = __DESTRUCTOR_COUNT__,
            segment = RODATA;
    CONDES: type    = interruptor,
            label   = __INTERRUPTOR_TABLE__,
            count   = __INTERRUPTOR_COUNT__,
            segment = RODATA,
            import  = __CALLIRQ__;
}
